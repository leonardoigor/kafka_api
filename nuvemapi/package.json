{
  "name": "kafka",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "up": "docker compose -f docker-compose.yml -p api_nuvem up -d --renew-anon-volumes ",
    "down": "docker compose -f docker-compose.yml -p api_nuvem down --volumes --rmi local"
  }
}